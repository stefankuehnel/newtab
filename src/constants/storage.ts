/**
 * Storage keys used for localStorage persistence
 * Centralized to prevent typos and make refactoring easier
 */
export const STORAGE_KEYS = {
  BOOKMARKS: "vite-ui-bookmarks",
  SERVICES: "vite-ui-services",
  THEME: "vite-ui-theme",
} as const;
